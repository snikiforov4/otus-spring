spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"
    username: sa
    password:
    schema: usr
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: none
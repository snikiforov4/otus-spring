spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:db;DB_CLOSE_DELAY=-1"
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: '/h2-console'
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
